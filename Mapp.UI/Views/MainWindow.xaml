<Window x:Class="Shmap.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mapp"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        xmlns:martinApp="clr-namespace:Mapp"
        xmlns:viewModels="clr-namespace:Shmap.ViewModels"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:command="http://www.galasoft.ch/mvvmlight"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="{Binding WindowHeight, Mode=TwoWay}" Width="{Binding WindowWidth, Mode=TwoWay}"
        WindowStartupLocation="Manual" WindowState="{Binding WindowState, Mode=TwoWay}" Top="{Binding WindowTop, Mode=TwoWay}" Left="{Binding WindowLeft, Mode=TwoWay}"
        d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel, IsDesignTimeCreatable=True}"
        DataContext="{Binding MainWindowVm, Source={StaticResource ViewModelLocator}}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <command:EventToCommand Command="{Binding Mode=OneWay, Path=WindowClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <FrameworkElement.Resources>
        <martinApp:ProductsListConverter x:Key="ProductsConverter"/>
        <martinApp:ProductsListToSkuConverter x:Key="ProductsToSkuConverter"/>
    </FrameworkElement.Resources>
    <TabControl>
        <TabItem Header="Amazon - konverter">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="20">
                    <Button Content="Zvolit fakturu" Width="150" Height="35" Command="{Binding SelectAmazonInvoicesCommand}"/>
                    <Label Content="Posledni existujici cislo v systemu:" HorizontalAlignment="Center"/>
                    <local:NumericTextBox  x:Name="ExistingInvoiceNum"
                                           Text="{Binding ExistingInvoiceNumber, TargetNullValue='', Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"
                                           MaxLength="9" HorizontalContentAlignment="Center" Height="23" VerticalAlignment="Center" Width="150">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Validation.HasError" Value="True">
                                        <Setter Property="Background" Value="Pink"/>
                                        <Setter Property="ToolTip"
                                                Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </local:NumericTextBox>
                    <Label Content="Default email:" HorizontalAlignment="Center"/>
                    <local:EmailTextBox Text="{Binding DefaultEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" MaxLength="30" HorizontalContentAlignment="Center" Height="23" VerticalAlignment="Center" Width="150" >
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Validation.HasError" Value="True">
                                        <Setter Property="Background" Value="Pink"/>
                                        <Setter Property="ToolTip"
                                                Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </local:EmailTextBox>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                        <Label Content="Otevrit cilovou slozku po konvertaci" HorizontalAlignment="Center" />
                        <CheckBox VerticalAlignment="Center" IsChecked="{Binding OpenTargetFolderAfterConversion, Mode=TwoWay}"/>
                    </StackPanel>
                    <Button Content="Konvertovat..." Width="150" Height="35" Margin="10" Command="{Binding ExportConvertedAmazonInvoicesCommand}"/>

                </StackPanel>
                <DataGrid Name="TopDataGrid" Grid.Row="1" Margin="5" AutoGenerateColumns="false" CanUserAddRows="false"
              ItemsSource="{Binding InvoiceItems}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Cislo (Amazon)" Width="0.6*" IsReadOnly="true" Binding="{Binding AmazonNumber}"/>
                        <DataGridTextColumn Header="Puvod" Width="0.4*" IsReadOnly="true" Binding="{Binding SalesChannel}"/>
                        <DataGridTextColumn Header="Nazev produktu (Amazon)" Width="*" Binding="{Binding AmazonProductName}">
                            <DataGridBoundColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Background" Value="Orange"/>
                                </Style>
                            </DataGridBoundColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="SKU (Amazon)" IsReadOnly="True" Width="*" Binding="{Binding AmazonSku}" />
                        <DataGridTextColumn Header="Kod zbozi (!)" Width="*" Binding="{Binding WarehouseProductCode}">
                            <DataGridBoundColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Background" Value="DarkOrange"/>
                                    <Style.Triggers>
                                        <Trigger  Property="Text" Value="----">
                                            <Setter Property="Background" Value="Red"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridBoundColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Mnozstvi v packu" Width="120" Binding="{Binding PackQuantityMultiplier}" />
                    </DataGrid.Columns>
                </DataGrid>
                <DataGrid Name="BottomDataGrid" Grid.Row="2" Margin="5" AutoGenerateColumns="false" CanUserAddRows="false"
              ItemsSource="{Binding Invoices}" >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Cislo (Amazon)" Width="0.6*" IsReadOnly="true"
                            Binding="{Binding AmazonNumber}"/>
                        <DataGridTextColumn Header="Puvod" Width="0.4*" IsReadOnly="true"
                            Binding="{Binding SalesChannel}"/>
                        <DataGridTextColumn Header="Produkty" Width="*" IsReadOnly="true" Binding="{Binding InvoiceProductNames, Converter={StaticResource ProductsConverter}}"/>
                        <DataGridTextColumn Header="SKU kody" Width="*" IsReadOnly="true" Binding="{Binding AmazonSkuCodes, Converter={StaticResource ProductsToSkuConverter}}"/>
                        <DataGridTextColumn Header="Kod z skladu (!)" Width="*" Binding="{Binding RelatedWarehouseSection}">
                            <DataGridBoundColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Background" Value="Orange"/>
                                    <Style.Triggers>
                                        <Trigger  Property="Text" Value="----">
                                            <Setter Property="Background" Value="Red"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridBoundColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Celni prohlaseni" Width="*" 
                                            Binding="{Binding CustomsDeclaration}">
                            <DataGridBoundColumn.ElementStyle>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="Background" Value="DarkOrange"/>
                                    <Style.Triggers>
                                        <Trigger  Property="Text" Value="----">
                                            <Setter Property="Background" Value="Red"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridBoundColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Typ VAT" Width="*"  IsReadOnly="True"
                                            Binding="{Binding VatType}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </TabItem>
        <TabItem Header="Konverze transakci">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="1">
                    <Button Content="Konvertovat transakce" Width="200" Height="35" Command="{Binding ConvertTransactionsCommand}" />
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="Autovyplnovani">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="1">
                    <Label Content="Nastaveni automatickeho vyplnovani (kod):" HorizontalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Label Content="RR" />
                        <TextBox MaxLength="6" Name="TrackingCodeBox" Width="100" Text="{Binding LatestTrackingCode, Mode=TwoWay}" TextAlignment="Center" VerticalContentAlignment="Center"></TextBox>
                        <Label Content="xxxCZ" />
                    </StackPanel>
                    <Label Content="Vkladani pomoci kombinace: (nejdrive)F2 + F4" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Row="2" Width="250" >
                    <Label Content="Vyzkouseni (dej kurzor do prniho policka):" />
                    <TextBox Margin="5" />
                    <TextBox Margin="5" />
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>

</Window>
